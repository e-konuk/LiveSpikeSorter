# Live Spike Sorter -- Python 3.10 environment.
#
#   python -m venv venv
#   venv\Scripts\activate
#   pip install -r requirements.txt
#
# Must be run from the repository root: the Kilosort line below is a relative path.

# CUDA build of PyTorch (not on PyPI).
--extra-index-url https://download.pytorch.org/whl/cu113
torch==1.12.0+cu113

# Kilosort 4.0.13, patched for LSS. See External/Kilosort/README.md.
# This REPLACES `pip install kilosort` -- the released package does not write
# the intermediate tensors (Wall3.npy, ctc.npy, ...) that LSS loads at run time.
# It pulls in Kilosort's own dependencies (numpy, scipy, scikit-learn, numba,
# faiss-cpu, tqdm) automatically.
./External/Kilosort

# Used by the launcher and the offline analysis scripts.
matplotlib
h5py
spikeinterface
probeinterface
pyqtgraph
PyQt5
qtpy
